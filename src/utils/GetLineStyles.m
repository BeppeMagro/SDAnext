function lineStyles = GetLineStyles()
% GetLineStyles - Returns a list of supported line styles for plotting.
%
% Outputs:
%   lineStyles - A cell array of line style options.

    lineStyles = {'-', '--', ':', '-.', 'none'};
end
