function markers = GetMarkers()
% GetMarkers - Returns a list of supported marker styles for plotting.
%
% Outputs:
%   markers - A cell array of marker options.

    markers = {'o', 's', 'd', '^', 'v', '>', '<', '*', '+', 'x', '.', 'none'};
end
