function models = GetModelsMap()
    % GetModelsMap - Returns a list of predefined model identifiers.
    %
    % Outputs:
    %   models - A cell array containing model identifiers.

    models = {'L', 'Q', 'LQ', 'LQC', 'LQL'};
end